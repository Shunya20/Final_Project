🧠 Pro Tip for Your Viva
Mention this in your explanation:

“If Nikto fails to return clean JSON, I’ve implemented a fallback that automatically switches to XML and saves the raw data instead of letting the scan crash. This makes the tool more robust in real-world scans.”

They’ll eat that up. 🍽️

